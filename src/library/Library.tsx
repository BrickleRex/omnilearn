// PLACEHOLDER — owned by the frontend-core agent. Replace entirely.
import type { Settings } from '../../shared/types';

export default function Library(_props: { settings: Settings; onSettings: (s: Settings) => void }) {
  return <div data-testid="library">Library placeholder</div>;
}
