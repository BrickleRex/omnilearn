// PLACEHOLDER — owned by the frontend-core agent. Replace entirely.
export default function MilestoneFlow(_props: { projectId: string; milestoneId: string }) {
  return <div data-testid="milestone-flow">Milestone flow placeholder</div>;
}
