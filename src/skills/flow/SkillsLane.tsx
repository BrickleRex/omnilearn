// PLACEHOLDER — owned by the skills-flow agent. The Library renders this when the
// track switch is on "Skills". Replace entirely (keep the export + props).
export default function SkillsLane(_p: { onNew: () => void }) {
  return <div data-testid="skills-lane">Skills lane placeholder</div>;
}
