// PLACEHOLDER — owned by the skills-make agent. Replace entirely (keep the export + props).
import type { Settings } from '../../../shared/types';
export default function SkillMake(_p: { skillId: string; moduleId?: string; settings: Settings; onSettings: (s: Settings) => void }) {
  return <div data-testid="skill-make">Skill make placeholder</div>;
}
