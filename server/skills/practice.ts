// PLACEHOLDER — owned by the practice backend agent. Replace entirely.
// Owns: calibration/grade, modules PATCH, drafts, run, hint, ghost, drills
// submit/attempts, ship/ships, chat + chat/stream for skills.
import { Router } from 'express';
export const skillsPracticeRouter: Router = Router();
