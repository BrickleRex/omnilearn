// PLACEHOLDER — owned by the research backend agent. Replace entirely.
// Owns: GET/POST /api/skills, GET/DELETE /api/skills/:id, PUT map, research
// job + stream, claims, sources (incl. user-pasted), refresh.
import { Router } from 'express';
export const skillsResearchRouter: Router = Router();
